const fs=require('fs');const env=fs.readFileSync('.env.local','utf-8');const url=env.match(/NEXT_PUBLIC_SUPABASE_URL=(.*)/)[1].trim();const key=env.match(/NEXT_PUBLIC_SUPABASE_ANON_KEY=(.*)/)[1].trim();const {createClient}=require('@supabase/supabase-js');const supabase=createClient(url,key);(async()=>{const {data:entities} = await supabase.from('account_entities').select('id, display_name, phone, status').in('display_name', ['정승호', '김민준']); const {data:roles} = await supabase.from('membership_roles').select('*').in('entity_id', entities.map(e=>e.id)); console.log('ENTITIES:', JSON.stringify(entities, null, 2)); console.log('ROLES:', JSON.stringify(roles, null, 2));})().catch(console.error);
