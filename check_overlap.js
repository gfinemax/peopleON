const fs=require('fs');const env=fs.readFileSync('.env.local','utf-8');const url=env.match(/NEXT_PUBLIC_SUPABASE_URL=(.*)/)[1].trim();const key=env.match(/SUPABASE_SERVICE_ROLE_KEY=(.*)/)[1].trim();const {createClient}=require('@supabase/supabase-js');const supabase=createClient(url,key);(async()=>{const {data:roles} = await supabase.from('membership_roles').select('*'); const counts={}; roles.forEach(r=>{if(!counts[r.entity_id])counts[r.entity_id]=[]; counts[r.entity_id].push(r.role_code);}); const overlap = Object.entries(counts).filter(([id, roles]) => roles.some(r=>r==='1차'||r==='등기조합원') && roles.some(r=>r==='예비조합원'||r==='예비')); console.log('OVERLAP:', overlap);})().catch(console.error);
